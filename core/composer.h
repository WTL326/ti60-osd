/* =============================================================================
 * L4 光栅化 —— 字符网格 → 画布像素（唯一的写画布者）
 * 语义锁定（C-4/决策树 R1）：**每次全量重画整个菜单区**，不做增量 patch。
 *   原因：透明底（无底色铺就要 diff 补0）+ 掩膜乒乓 → 增量会露出上一块的旧内容。
 * 无 multiplicative 分支：字库查不到由 scene_build 提前换成 '?'，此处不存在失败路径。
 * =============================================================================
 */
#ifndef OSD_COMPOSER_H
#define OSD_COMPOSER_H

#include <stdint.h>

/* 渲染 osd_buf → 当前后台块。全量覆盖 cols 0..543 的所有扫行。 */
void composer_render(void);

#endif /* OSD_COMPOSER_H */
